// UWM logo — served by Frappe from the app's public/frontend folder. Using an
// absolute URL keeps it correct whether the app is loaded at /waste_management_ujjain/m
// (thin-shell APK) or anywhere else on the same origin.
export const LOGO = '/assets/waste_management_ujjain/frontend/uwm-logo.jpg'
